# Vanish Module
vanish:
  enabled: true
  hide-armor: true                # true = fully hidden from others (including armor), false = "ghost mode" (visible, only tab-hidden + protection)
  tab-hidden: true                # Remove vanished players from other players' tab list. Only effective with hide-armor: false (ghost mode) — a fully hidden entity is always removed from the tab (Paper API limitation). In ghost mode the entry is re-removed periodically, so it may briefly reappear on distance changes.
  persist-on-rejoin: true         # Keep the vanish state across a rejoin
  no-collision: true              # Let other players walk through the vanished player
  no-step-sound: true             # Suppress step sounds for other players
  invincible: false               # true = vanished player takes no damage at all
  projectiles-pass-through: true  # Arrows & co. don't hit vanished players
  mobs-ignore: true               # Mobs don't target or look at vanished players
  hide-join-quit-messages: true   # No join/quit messages for vanished players
  op-see: true                    # OPs (with plainbase.vanish.see) can see vanished players

  world:
    enabled: true                 # /vanish world
  all:
    enabled: true                 # /vanish all

  commands:
    vanish:
      enabled: true

# DO NOT EDIT BELOW UNLESS YOU KNOW WHAT YOU ARE DOING
version: 1.0